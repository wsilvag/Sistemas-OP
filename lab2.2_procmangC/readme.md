#  README: ProcMan - Gestor de Procesos Básico

Este proyecto implementa un gestor de procesos interactivo (`ProcMan`) en C que permite crear, rastrear, listar, terminar y visualizar la jerarquía de procesos hijos en segundo plano.

##  1. Información del Autor

| Campo | Detalle |
| :--- | :--- |
| **Nombre:** | [Walter Silva] |
| **ID de Estudiante:** | [5105807] |
| **Correo Electrónico:** | [wallepro39@gmail.com] |
| **Repositorio:** | `https://github.com/wsilvag/Sistemas-OP.git` |

---

## ⚙️ 2. Instrucciones de Compilación

El proyecto utiliza un `Makefile` estándar para automatizar el proceso de compilación con `gcc`.

1.  Asegúrate de que las herramientas de compilación (`gcc` y `make`) estén instaladas (resuelto previamente con `build-essential`).
2.  Navega al directorio que contiene los archivos:

    ```bash
    cd Sistemas-OP/lab2.2-basic-procmanage-c/homework1
    ```

3.  Ejecuta el comando `make` para compilar el código fuente en el ejecutable binario `procman`:

    ```bash
    make
    ```

4.  Si la compilación es exitosa, el archivo ejecutable `./procman` estará disponible.

---

## 🚀 3. Instrucciones de Uso

Para iniciar el gestor de procesos interactivo, ejecuta el binario compilado:

```bash
./procman
